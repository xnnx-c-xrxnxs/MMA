# Closing-keyword demo

This file exists only to demonstrate how a PR auto-links (and, on the default
branch, auto-closes) an issue via a closing keyword such as `Closes #27`.

Safe to delete along with the demo PR and issue #27.
