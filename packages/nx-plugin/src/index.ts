// @mma/nx-plugin — internal generators for domain scaffolding
// This package is consumed via `pnpm nx g @mma/nx-plugin:{generator}`.
// Public exports are the generator factories (referenced from generators.json).
export {};
