{
  "name": "@old-st/design-tokens",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Cross-platform design tokens (colors, spacing, radii, typography) — single source of truth for @old-st/ui (via CSS variables) and @old-st/mobile-ui (via JS imports)",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/lib/tokens.css"
  }
}
