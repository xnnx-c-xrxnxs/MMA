{
  "name": "@mma/aws-secrets",
  "version": "1.0.0",
  "description": "AWS Secrets Manager resolver — reads project-level secret at Lambda cold-start and populates process.env.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1011.0"
  }
}
